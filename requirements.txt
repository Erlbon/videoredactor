# Python dependencies for The Ʌideo Redactor.
#
# NOT included here (external binaries, not pip packages -- the app
# shells out to these, same "real CLI tool" philosophy as the epub
# tool's Calibre integration):
#   - ffmpeg / ffprobe   (thumbnails, remux)      https://ffmpeg.org/download.html
#   - MKVToolNix         (mkvpropedit, mkvmerge)  https://mkvtoolnix.download/downloads.html
# Both must be installed separately and available on PATH.

PyQt6>=6.6
mutagen>=1.47

# Shared UI/core code with the epub and mp3 Redactor tools -- a real
# pip dependency (github.com/Erlbon/redactor_common), not a vendored
# copy: previously each of the three projects hand-copied this file
# tree in separately, so a fix to shared code needed three separate
# manual resyncs and could silently drift out of sync (which is exactly
# what happened -- see that repo's README/commit history around the
# setWindowModality crash fix). Pinned to a tag, not @main -- see that
# repo's README ("Releasing a new version") for how to bump this pin
# deliberately when it changes, rather than floating and having one bad
# push there break this project's next install.
redactor_common @ git+https://github.com/Erlbon/redactor_common.git@2026-09-06-06

# For building the .exe:
pyinstaller>=6.3
